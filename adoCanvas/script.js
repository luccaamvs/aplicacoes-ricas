// Primeira coisa a fazer é pegar o Canvas e o configurar.

window.addEventListener("load", () => {
    const canvas = document.querySelector('#meuCanvas');

//Definir context e o definir como 2D:

    const ctx = canvas.getContext('2d');

ctx.fillRect
});

